# 2026-03-25 — Daily Notes

## First conversation with J Tanui (@jus_tanui)
- Telegram user, first session today
- Wants to build a POS system for small retail shops/dukas in Kenya
- $400 one-time license, software-only (no hardware bundle)
- Offline-first (internet unreliable in Kenya)
- Thermal receipt printer via USB
- M-Pesa integration + cash
- Simple: sales, inventory, receipts — no restaurant features
- Target: 6-8 week timeline to shippable MVP

## Decisions confirmed
- Name: "Duka POS"
- Platform: Electron desktop, Windows-first
- Tax: configurable from Day 1 (default 16% Kenya VAT)
- M-Pesa: Daraja sandbox hook (not simulated)
- Reports: CSV export for end-of-day
- No custom branding yet

## Execution Log
- Scaffolding detailed in `planning/pos-mvp/`.
- Repo structure set up in `projects/duka-pos/`.
- `init.js` updated to seed 7 sample products (Milk, Bread, Sugar, etc.) and default settings.
- `main.js` wired with real `better-sqlite3` DB, IPC handlers, and conditional dev/prod loading.
- `src/ipc/products.js` & `src/ipc/sales.js` implemented with real transactions and inventory logic.
- `src/ui/SalesRegister.jsx` created as a functional React component (grid + cart).
- `src/renderer/App.jsx` wired to fetch from IPC `products:list` and submit to `sales:create`.
- `index.html` points to the React entry point.
- Git commit `8f8de4f` captures the MVP state.

## Next immediate steps
- Verify M-Pesa integration (currently mocked in `src/ipc/mpesa.js`) against a real Daraja sandbox.
- Implement `src/printer/escpos.js` with `node-thermal-printer` logic.
- Test packaging for Windows (`npm run build`).